apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Local Docker/Kubernetes (Docker Desktop, Rancher Desktop, etc.): use an image
# built on the same Docker engine without pushing to a registry.
resources:
  - ../../base

patches:
  - path: patch-deployment.yaml
